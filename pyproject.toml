[project]
name = "intergalactic-pizza"
version = "0.1.0"
description = "Learning Sandbox"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12.5",
]

[project.scripts]
intergalactic-pizza = "intergalactic_pizza:main"

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"
